<!DOCTYPE html>
<html>

  <head>
    <meta charset='utf-8'>

    <link rel="stylesheet" type="text/css" media="screen" href="../stylesheets/stylesheet.css">
    <script type="text/javascript"
      src="../javascripts/scale.fix.js">
    </script>
    <script type="text/javascript"
      src="http://cdn.mathjax.org/mathjax/latest/MathJax.js?config=TeX-AMS-MML_HTMLorMML">
    </script>
    <title>GAP package {{ site.data.@@package@@.name }}</title>
  </head>

  <body>

    <!-- HEADER -->
    <div id="header_wrap" class="outer">
        <header class="inner">
          {% if site.github.repository_url %}
          <a id="forkme_banner" href="{{ site.github.repository_url }}">View On GitHub</a>
          {% elsif site.data.@@package@@.github %}
          <a id="forkme_banner" href="{{ site.data.@@package@@.github }}">View On GitHub</a>
          {% endif %}

          <h1 id="project_title">{{ site.data.@@package@@.name }}</h1>
          <h2 id="project_tagline">{{ site.data.@@package@@.description }}</h2>
          <h3 id="project_authors">{{ site.data.@@package@@.version }}</h3>

            <section id="downloads">
              {% for item in site.data.@@package@@.downloads %}
                {% if item.name == ".tar.gz" %}
                    <a class="tar_download_link" href="{{ item.url }}">Download {{ item.name }}</a>
                {% else %}
                    <a class="zip_download_link" href="{{ item.url }}">Download {{ item.name }}</a>
                {% endif %}
              {% endfor %}
            </section>
            
        </header>
    </div>

    <!-- MAIN CONTENT -->
    <div id="main_content_wrap" class="outer">
      <section id="main_content" class="inner">
      
      {{ content }}
      
      </section>
    </div>

    <!-- FOOTER  -->
    <div id="footer_wrap" class="outer">
      <footer class="inner">
        <p class="copyright"> Maintained by: 
          {% for person in site.data.@@package@@.maintainers %}
            {% if person.url %}
              <a href="{{ person.url }}">{{ person.name }}</a>{% else %}
              {{ person.name }}{% endif %}{% unless forloop.last %}, {% endunless %}
          {% endfor %}
        </p>
        <p>Published with <a href="https://pages.github.com">GitHub Pages</a></p>
      </footer>
    </div>

    

  </body>
</html>
